<h1>Urly Urls</h1>

<% @urls.each do |url| %>

<p>
  <%= link_to url.random_id, url_path(id: url.random_id) %> >goes to< <%= url.original%>
</p>
<% end %>

<%= link_to "Shorten another Url", new_url_path  %>
